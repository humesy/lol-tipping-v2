<div class="navbar">
  <%= image_tag asset_path('icon.png'), class: "navbar-icon" %>

  <div class="navbar-right">
    <% if current_user %>
      <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
    <% else %>
      <%= link_to "Sign Up", new_user_registration_path %>
    <% end %>
  </div>
</div>